<thead>
    <tr>
        <th class="text-nowrap">{{ 'table.col.id'|trans({}, 'dashboard') }}</th>
        <th class="text-nowrap">{{ 'table.col.username'|trans({}, 'dashboard') }}</th>
        <th class="text-nowrap">{{ 'table.col.email'|trans({}, 'dashboard') }}</th>
        <th class="text-nowrap">{{ 'tables_wide.col.company'|trans({}, 'ui') }}</th>
        <th class="text-nowrap">{{ 'table.col.role'|trans({}, 'dashboard') }}</th>
        <th class="text-nowrap">{{ 'table.col.status'|trans({}, 'dashboard') }}</th>
        <th class="text-nowrap">{{ 'tables_wide.col.city'|trans({}, 'ui') }}</th>
        <th class="text-nowrap">{{ 'tables_wide.col.phone'|trans({}, 'ui') }}</th>
        <th class="text-nowrap">{{ 'tables_wide.col.last_access'|trans({}, 'ui') }}</th>
        <th class="text-nowrap">{{ 'tables_wide.col.notes'|trans({}, 'ui') }}</th>
        <th class="text-nowrap">{{ 'table.col.actions'|trans({}, 'dashboard') }}</th>
    </tr>
</thead>
